name: Python Unit Tests

on: [push]

jobs:
  run-unit-tests:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - name: Install Python Dependencies
        run: |
          pip install -r requirements.txt
          sudo find /usr/local/lib/python*/dist-packages -type d -exec chmod 755 {} +
          sudo find /usr/local/lib/python*/dist-packages -type f -exec chmod 755 {} +
          sudo find /usr/local/lib/python*/dist-packages -type f -name '*.py*' -exec chmod 644 {} +
          sudo find /usr/local/lib/python*/dist-packages -type f -name '*.pth' -exec chmod 644 {} +
      - name: Install Tokenizer Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang
      - name: Run Unit Tests
        run: |
          cd tests/unittest
          python3 -m unittest discover
